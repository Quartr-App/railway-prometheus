global:
  scrape_interval:     15s # By default, scrape targets every 15 seconds.

  external_labels:
    monitor: 'railway-prom'

scrape_configs:
  - job_name: 'api'
  scrape_interval: 5s
  scheme: https
  metrics_path: /metrics
  static_configs:
    - targets: ['public-api.railway.internal']

